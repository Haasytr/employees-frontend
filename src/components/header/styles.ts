import { styled } from "@/styles";

export const Container = styled('div', {
    width: '100%',
    backgroundColor: '$gray_500',
    padding: '1rem 0',
    textAlign: 'center',
    color: '$gray_100'
})